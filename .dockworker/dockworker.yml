dockworker:
  version: '3.0'
  application:
    name: drupal.redis.lib.unb.ca
    finish_marker: 'Ready to accept connections'
    project_prefix: IN
    upstream_images:
      - redis:alpine
  deployment:
    environments:
      - dev
      - prod
  github:
    repo: drupal.redis.lib.unb.ca
    owner: unb-libraries
  image:
    push_branches:
      - dev
      - prod
    name: unblibraries/drupal.redis.lib.unb.ca
    date_tag_image: true
    repository: dockercloud
